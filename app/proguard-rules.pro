# Minification is off for release builds (see app/build.gradle.kts), so nothing here is
# active yet. Turning it on later needs the Compose and, once it lands, Room keep rules
# verified on a device rather than trusted because the build stayed green.
